import React from 'react';

const TechBlogPage = ({ params, searchParams }) => {
   console.log(searchParams);
   console.log(params);
   return <div>Tech Blog Page</div>;
};

export default TechBlogPage;
